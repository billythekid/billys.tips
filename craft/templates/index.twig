{% extends "_layout" %}

{% block content %}

  {{ entry.body }}

  {% set tip = craft.entries.section('tips').first() %}
  {% if tip %}
    <div class="row">
      <h2>Latest Tip</h2>
      <hr class="heading-line">
      <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2 col-lg-6 col-lg-offset-3">
        {% include '_partials/tipCard' %}
      </div>
    </div>
  {% endif %}

{% endblock %}
