{% if tip is not defined and entry is defined %}
  {% set tip = entry %}
{% endif %}
{% if tip is defined %}
  <div class="latest-tips-tip">
    <p class="date">{{ tip.postDate.format('F d, Y') }}</p>
    <p>{{ tip.link }}</p>
    <p>{{ tip.intro|raw }}</p>
  </div>
{% endif %}